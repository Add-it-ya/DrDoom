# Root-cause classifier

Generated by `python -m drdoom.classify.train`.

The classifier runs only after detection has flagged a window, so it is trained and
scored on anomalous windows alone. Scores are reported per incident: window
predictions are pooled by majority vote, because one verdict per incident is what an
engineer acts on and because windows cut from the same outage are not independent
observations.

Cross-validation folds are grouped by incident. A random fold split would put
overlapping slices of the same outage in both training and validation and report
memorisation as skill.

## Two label sets, never pooled

**Synthetic** carries a true causal label on every incident, because the generator
chose the cause before injecting it. This is where a claim about root-cause
classification can actually be tested.

**Real telemetry** does not. The Server Machine Dataset records which of its 38
dimensions deviated during each incident but publishes no mapping from dimension to
meaning, so calling a group of dimensions "memory" would be invention. Its labels
here are signature archetypes: incidents clustered by the shape of their deviation,
named for how much of the metric space they touch. That makes the real-data task
subsystem-signature attribution rather than causal analysis, which is a weaker claim
and the honest one.

The archetype taxonomy is derived from the annotation file across all incidents, not
from metric values, so it hands the model no signal it could read off its inputs.
With 325 incidents in total there is not enough data to define the taxonomy on a
subset and have it stay stable.

## What the numbers say

- **smd**: pooling windows into one verdict per incident moves macro F1 from 0.580 to 0.827 (+0.246). Several windows voting on the same incident cancel individual mistakes.
- **synthetic**: pooling windows into one verdict per incident moves macro F1 from 0.953 to 0.997 (+0.044). Several windows voting on the same incident cancel individual mistakes.

Synthetic scores 0.997 against 0.827 on real telemetry. The gap is not evidence that the model is better at synthetic incidents; it is evidence that synthetic incidents are separable by construction. The generator picks a cause and writes its signature directly into the metrics, so a near-perfect score there measures the generator more than the classifier. The real number, on fewer and messier incidents, is the one to quote.

## smd

- Classes: `broad_signature`, `narrow_signature`
- Test incidents: 59 (from 1001 windows)
- **Event macro F1: 0.827** (95% CI [0.726, 0.915])
- Event accuracy: 0.831
- Window macro F1: 0.580 (secondary)
- Grouped cross-validation macro F1 during tuning: 0.744
- Smallest class support in any split: 28 incidents

### Incidents per class in the test split

| Class | Incidents |
|---|---:|
| `broad_signature` | 29 |
| `narrow_signature` | 30 |

### Confusion matrix, per incident

| actual \ predicted | broad_signature | narrow_signature |
|---|---:|---:|
| **broad_signature** | 29 | 0 |
| **narrow_signature** | 10 | 20 |

### Most informative features

| Feature | Importance |
|---|---:|
| `m24_std` | 0.1016 |
| `m30_std` | 0.0619 |
| `m23_std` | 0.0282 |
| `m37_std` | 0.0233 |
| `m10_std` | 0.0193 |
| `m03_std` | 0.0179 |
| `m30_mean` | 0.0175 |
| `m00_std` | 0.0172 |

### Archetype definitions

| Archetype | Incidents | Dominant dimensions |
|---|---:|---|
| `narrow_signature` | 181 | 13, 9, 14, 12, 15, 10, 11 |
| `broad_signature` | 109 | 1, 31, 21, 22, 28, 19, 20, 2 |
| `other` | 37 | n/a |

Dimensions are one-based indices into the 38 metric columns. The dataset
publishes no names for them, so none are invented here.

## synthetic

- Classes: `deploy_regression`, `downstream_outage`, `memory_leak`, `traffic_spike`
- Test incidents: 300 (from 5014 windows)
- **Event macro F1: 0.997** (95% CI [0.989, 1.000])
- Event accuracy: 0.997
- Window macro F1: 0.953 (secondary)
- Grouped cross-validation macro F1 during tuning: 0.945
- Smallest class support in any split: 43 incidents

### Incidents per class in the test split

| Class | Incidents |
|---|---:|
| `deploy_regression` | 72 |
| `downstream_outage` | 78 |
| `memory_leak` | 72 |
| `traffic_spike` | 78 |

### Confusion matrix, per incident

| actual \ predicted | deploy_regression | downstream_outage | memory_leak | traffic_spike |
|---|---:|---:|---:|---:|
| **deploy_regression** | 72 | 0 | 0 | 0 |
| **downstream_outage** | 1 | 77 | 0 | 0 |
| **memory_leak** | 0 | 0 | 72 | 0 |
| **traffic_spike** | 0 | 0 | 0 | 78 |

### Most informative features

| Feature | Importance |
|---|---:|
| `queue_depth_std` | 0.3284 |
| `error_rate_pct_max` | 0.2105 |
| `error_rate_pct_std` | 0.0760 |
| `latency_ms_std` | 0.0752 |
| `queue_depth_max` | 0.0618 |
| `error_rate_pct_mean` | 0.0423 |
| `error_rate_pct_min` | 0.0285 |
| `latency_ms_slope` | 0.0246 |
